# This is the branch where I am storing my GlaucoSense related work:


## 1> "9 Sept 2026/Real_ESRGAN" :

In the folder "9 Sept 2026/Real_ESRGAN" we are using the model "RealESRGAN_x2plus.pth" and the script "Real.py" to use the model to improve the quality of the fundus images. We have download the model Real_ESRGAN for the GitHub source "https://github.com/xinntao/Real-ESRGAN" and its doc page "https://github.com/xinntao/Real-ESRGAN/blob/master/docs/model_zoo.md" where we got the "RealESRGAN_x2plus.pth" model. Earlier we were using the "RealESRGAN_x4plus.pth" model but it was giving an issue as the model was converting the images to paint like images. Inside this folder, use the folder name "Working_Testing_NEW" the model and the working script is in this folder only.
